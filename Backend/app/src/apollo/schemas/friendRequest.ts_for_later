import gql from "apollo-server-express";

export const typeDef = gql`
    extend type Query {
        friendRequests(data: friendRequestsInput): [ friendRequest ]!
    }

    input friendRequestsInput {
        userID: ID!
    }
`;